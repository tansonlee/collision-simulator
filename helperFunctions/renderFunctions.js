const drawBalls = () => {
	ball1.render();
	ball2.render();
};

const updateBalls = () => {
	ball1.update();
	ball2.update();
};

const drawDisplayBoxes = () => {
	ballDisplay1.render();
	ballDisplay2.render();
};
